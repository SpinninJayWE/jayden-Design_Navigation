{
  "compilerOptions": {
    "target": "esnext",                    // 编译到的目标语言版本
    "module": "esnext",                    // 使用的模块系统
    "moduleResolution": "node",            // 模块解析策略
    "jsx": "preserve",                     // 对 JSX 代码使用保留模式，由框架的构建工具进一步处理
    "esModuleInterop": true,               // 允许默认导入非ES模块
    "strict": true,                        // 启用所有严格类型检查选项
    "forceConsistentCasingInFileNames": true, // 强制文件名大小写一致性
    "noImplicitReturns": true,             // 不允许函数没有返回值
    "noImplicitAny": true,                 // 不允许隐含 any 类型
    "noImplicitThis": true,                // 不允许隐含的 this 表达式
    "alwaysStrict": true,                  // 以严格模式解析并为每个源文件写入 "use strict"
    "skipLibCheck": true,                  // 跳过声明文件（.d.ts）的类型检查
    "resolveJsonModule": true,             // 允许导入 json 模块
    "lib": ["dom", "esnext"],              // 编译时需要引入的库文件列表
    "baseUrl": ".",                        // 基础目录，用于解析非相对模块名
//    "paths": {                             // 设置路径映射，可以设置别名
//      "@/*": ["src/*"]
//    },
//    "outDir": "./dist",                    // 输出目录
    "sourceMap": true                      // 生成相应的 '.map' 文件
  }
}
